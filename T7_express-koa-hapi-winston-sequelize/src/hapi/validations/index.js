const BookingValidator = require('./booking.validation');
const EventValidator = require('./event.validation');
const UserValidator = require('./user.validation');

module.exports = {
  BookingValidator,
  EventValidator,
  UserValidator
}